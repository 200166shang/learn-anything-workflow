# Xiaoe/XiaoeTong

Use the existing persistent-browser capture with the user's authorized session. Pause for login, playback, CAPTCHA, DRM, or paid access. Store the browser session under the canonical library runtime directory, never inside each item package.
