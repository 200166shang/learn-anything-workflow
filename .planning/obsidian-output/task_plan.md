# 任务计划：将视频学习输出迁入 Obsidian Vault

## 目标
将现有 `outputs` 迁入 `/Users/syz/code/obsidian_本地知识库/编程开发/网站视频转录`，使项目脚本和 video-learning skill 默认在该根目录生成完整学习包，并用用户给出的 B 站视频验证。

## 当前阶段
阶段 1：需求与发现

## 各阶段

### 阶段 1：需求与发现
- [x] 确认 Vault 路径和目标结构。
- [x] 审查脚本中输出根目录的来源，以及现有输出的迁移范围。
- **状态：** complete

### 阶段 2：迁移与实现
- [x] 安全迁移现有 `outputs` 目录，不覆盖 Vault 内容。
- [x] 为正式入口提供统一的默认输出根目录。
- [x] 更新 video-learning skill 和 README。
- **状态：** complete

### 阶段 3：验证
- [x] 用指定 B 站 URL 下载完整源视频并运行学习包入口。
- [x] 核对生成文件、路径和相对引用。
- **状态：** complete（验证片段；完整视频转写需在非受限终端继续）

### 阶段 4：交付
- [x] 记录变更和验证结果。
- **状态：** complete

## 已做决策
| 决策 | 理由 |
|------|------|
| 整个输出根目录进入 Vault | 笔记、截图和字幕保持原有相对路径；无需同步或复制。 |
| 输出根目录为 Vault 下已有的 `编程开发/网站视频转录` | 该目录当前为空，名称符合用途，且不混入项目源代码。 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `rmdir outputs` 遇到 `.DS_Store` | 1 | 将该 Finder 元数据一并迁入目标根目录后删除空目录。 |
| 受控终端在约 30 秒后截断完整 Whisper 转写 | 2 | 使用同一视频前 20 秒完成端到端路径验证；不将其描述为完整学习笔记。 |
| skill 校验器拒绝既有 `compatibility` frontmatter | 1 | 此字段在本次改动前已存在；未为通过校验而改变该 skill 的既有元数据。 |
