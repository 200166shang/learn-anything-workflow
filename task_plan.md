# convert_voice_to_article 任务计划

目标：在本目录实现一个适用于 macOS Apple Silicon 的小工具，针对用户有权访问的小鹅通视频，完成浏览器捕获/下载、音频提取、本地语音转文字，并将转写整理为可交给 AI 继续理解的 Markdown 文章。

## 阶段

- [completed] 1. 检查 XET-Video-Downloader 与目标页面的可用性
- [completed] 2. 设计并实现独立的 `convert_voice_to_article`
- [completed] 3. 安装/检查本机依赖并做静态与最小运行验证
- [completed] 4. 用目标视频跑通下载和转写；如需用户登录，保留清晰的接续步骤
- [completed] 5. 整理输出文件与使用说明
- [completed] 6. 为单个已有小鹅通视频实现截图候选与本地审阅闭环
- [completed] 7. 用 `11.10-机器人TF和里程计计算流程` 验证候选截图、Codex 预筛和确认结果
- [completed] 8. 生成单视频学习笔记输入包与自测题输入包
- [completed] 9. 重跑一个 B 站 STM32 分集并检查/整理学习笔记（不生成测试题）
- [completed] 10. 为 B 站正式字幕增加本地缓存、批量抽帧和审阅闭环
- [pending] 11. 将 Codex 筛选后的截图证据进一步自动合并进单视频 Markdown
- [completed] 12. 为第十二章 24 个已完成小鹅通视频生成候选画面、预筛 JSON、证据输入包和合规单视频笔记
- [completed] 13. 逐包校验第十二章的截图、时间戳与 Markdown 引用，并生成章节汇总

## 约束与判断

- 只处理用户自己有权访问的视频；不绕过 DRM 或访问控制。
- 不收集、保存或要求用户提供账号、密码、Cookie、token。
- 首次捕获需要用户在本地弹出的持久化浏览器中自行登录。
- 转写采用 faster-whisper；截图筛选和最终笔记生成由 Codex 完成，避免强制绑定本地或云端大模型。
- 第一版以“能成功跑通”为验收标准，优先复用已有 MP4、SRT 和 TXT，不重复下载或转写。
- 单视频先于章节汇总；截图采用固定间隔、字幕锚点、场景变化和相似度去重的混合候选策略。
- B 站统一使用一个入口：自动缓存或复用 `source.mp4`，随后始终从本地视频批量抽帧；浏览器只在下载失败时内部兜底。
- Codex 只做视觉预筛，用户在本地 HTML 审阅页拥有最终确认权。
- 第一版 AI 交接采用 Markdown/JSON 输入包，不绑定 API 或具体模型。
- Codex 先查看联系表完成视觉预筛，用户在 HTML 审阅页拥有最终确认权；确认结果保存为 JSON。
- 最终笔记只取确认截图附近的带时间戳字幕，并追加确定性的证据索引；不自动生成自测题。

## 遇到的错误

| 已处理 | 说明 |
|---|---|
| Pillow 弃用警告 | 使用 `tobytes()` 生成灰度签名，避免 Pillow 12 的 `getdata()` 警告 |
| OCR 不可用 | 本机没有 tesseract，候选流程继续运行，OCR 保持为空 |

## 第十二章执行约束

- 范围严格限定为 `第十二章-机器人/自动驾驶-传感器优化处理-雷达-相机` 的 24 个现有本地视频；不重新下载或转写。
- 每个视频必须按 `video-learning` 的固定顶层模板生成 `notes/notes.md`，并写入 `review/codex_prescreen.json` 后再刷新 `notes/notes_input.md`。
- 截图仅作证据补充：每个保留画面需要紧邻其支持的正文结论；所有重要结论都必须含可解析的 SRT 时间链接。
- 所有单视频笔记完成后，才能新增第十二章汇总。
